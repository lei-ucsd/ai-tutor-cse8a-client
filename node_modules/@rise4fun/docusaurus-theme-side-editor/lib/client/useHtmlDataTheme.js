import useIsBrowser from '@docusaurus/useIsBrowser';
export default function useHtmlDataTheme() {
    const isBrowser = useIsBrowser();
    const colorMode = isBrowser ? document.firstElementChild.dataset.theme : 'dark';
    return colorMode;
}
