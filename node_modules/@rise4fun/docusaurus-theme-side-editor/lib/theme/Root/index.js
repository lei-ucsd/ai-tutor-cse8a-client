import React from 'react';
import Root from '@theme-original/Root';
import { SplitEditorProvider } from '../../client/SideEditorContext';
// Root that mounts SplitEditorProvider in the React tree
export default function RootWrapper(props) {
    const { children } = props;
    if (Root === RootWrapper)
        return React.createElement(SplitEditorProvider, null, children);
    else
        return (React.createElement(SplitEditorProvider, null,
            React.createElement(Root, null, children)));
}
