import clsx from 'clsx';
import React from 'react';
import { PanelResizeHandle } from 'react-resizable-panels';
import styles from './ResizeHandle.module.css';
export default function ResizeHandle({ className = '', collapsed = false, direction = 'horizontal', id, }) {
    return (React.createElement(PanelResizeHandle, { className: clsx(styles.ResizeHandleOuter, className), id: id },
        React.createElement("div", { className: styles.ResizeHandleInner, "data-collapsed": collapsed || undefined, "data-direction": direction }, "\u28FF")));
}
